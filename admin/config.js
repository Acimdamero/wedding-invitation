/**
 * Admin dashboard Supabase configuration (anon key — auth session grants SELECT via RLS).
 * Use the same project URL and anon key as js/config.js.
 */
window.ADMIN_CONFIG = {
  slug: 'erzal-dhea',
  supabaseUrl: 'YOUR_SUPABASE_URL',
  supabaseAnonKey: 'YOUR_SUPABASE_ANON_KEY',
  siteUrl: 'https://erzal-dhea-wedding.vercel.app/',
};
